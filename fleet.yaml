defaultNamespace: monitoring

helm:
  releaseName: prometheus-agent
  chart: prometheus-agent
  repo: https://prometheus-community.github.io/helm-charts
  version: 1.0.0 # або конкретну версію, що підтримує агент-режим
  valuesFiles:
    - values.yaml
